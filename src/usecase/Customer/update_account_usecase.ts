import result from "src/domain/common/result";
import { Customer } from "src/domain/model";

class Update_account {
  // async action({
  //   uuid,
  //   updated_customer,
  // }: {
  //   uuid: string;
  //   updated_customer: Omit<Customer, "orders" | "uuid" | "profile_image">;
  // }): Promise<result> {}
}

export default Update_account;
